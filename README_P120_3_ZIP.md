# phase120-3.zip

Phase 120.3 - Master Agent Router Handoff

Run:

```powershell
node scripts/p120-3.cjs
npm run phase120:3:verify
npm run build
```

Optional smoke, only when frontend runs on port 3000:

```powershell
npm run phase120:3:smoke
```

Main local test page:

```text
http://localhost:3001/cmt/master
```
