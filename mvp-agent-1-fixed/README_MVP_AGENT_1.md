# mvp-agent-1-fixed

Corrected ZIP layout. Extract into project root, then run:

```powershell
node scripts/mvp-agent-1.cjs
npm run mvp:verify
npm run build
```

Route after restart:

```text
/cmt/master/secure/agent
```
