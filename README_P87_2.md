# Phase 87.2

Short-name dashboard/smoke patch.

Run:

```powershell
node scripts/p87-2.cjs
npm run phase87:2:verify
npm run build
```

Optional smoke if frontend runs on localhost:3000:

```powershell
npm run phase87:2:smoke
```
