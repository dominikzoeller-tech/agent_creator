# Phase 14.2 – Cockpit Next Actions / Guided Flow

Dieses Paket enthält Patch- und Verify-Script für Phase 14.2.

Ausführen im Projektroot:

```powershell
node scripts/phase14-2-patch-cockpit-next-actions-guided-flow.cjs
npm run phase14:2:verify
npm run build
```

Docker-Neustart nur, wenn du `/master-cockpit` direkt im Container testen willst.
