# phase120-0.zip

Phase 120.0 - Master Agent Router MVP

Run:

```powershell
node scripts/p120-0.cjs
npm run phase120:0:verify
npm run build
```

If frontend already runs, open:

```text
http://localhost:3001/cmt/master
```

Status: master-router-local-testable, not live with model yet.
