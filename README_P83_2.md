# Phase 83.2

Short-name dashboard/smoke patch.

Run:

```powershell
node scripts/p83-2.cjs
npm run phase83:2:verify
npm run build
```

Optional smoke if frontend runs on localhost:3000:

```powershell
npm run phase83:2:smoke
```
