const fs=require('fs');const path=require('path');const root=process.cwd();const page=fs.readFileSync(path.join(root,'frontend/app/cmt/master/secure/agent/page.tsx'),'utf8');let ok=true;for(const token of ['Arbeitsmodus','Confidence','Warum diese Einordnung','Haupttestpunkt']){if(!page.includes(token)){console.error('[missing]',token);ok=false}else console.log('[ok]',token)}process.exit(ok?0:1);