const fs=require('fs');const path=require('path');const root=process.cwd();const must=['frontend/lib/cmt-secure-master-agent-mvp.ts','frontend/app/cmt/master/secure/agent/page.tsx','scripts/mvp-agent-1.cjs','scripts/v-mvp-agent-1.cjs'];let ok=true;for(const rel of must){if(!fs.existsSync(path.join(root,rel))){console.error('[missing]',rel);ok=false}else console.log('[ok]',rel)}const pkg=JSON.parse(fs.readFileSync(path.join(root,'package.json'),'utf8'));for(const key of ['mvp:verify','agent:mvp:verify']){if(!pkg.scripts?.[key]){console.error('[missing script]',key);ok=false}else console.log('[ok script]',key)}if(ok)console.log('[OK] mvp agent verify passed');process.exit(ok?0:1);