const fs = require("fs");
const path = require("path");
function full(file){ return path.join(process.cwd(), file); }
function exists(file){ return fs.existsSync(full(file)); }
function read(file){ return fs.readFileSync(full(file), "utf8"); }
function write(file, content){ fs.mkdirSync(path.dirname(full(file)), { recursive: true }); fs.writeFileSync(full(file), content, "utf8"); }
function ensureFile(file, content){ if(!exists(file)){ write(file, content); console.log(`OK ${file}: erstellt.`); } else { console.log(`SKIP ${file}: existiert bereits.`); } }
function patchPackage(){
  const file="package.json";
  const pkg=JSON.parse(read(file));
  pkg.scripts=pkg.scripts||{};
  pkg.scripts["phase11:4:patch"]="node scripts/phase11-4-patch-missing-tool-capability-request-flow.cjs";
  pkg.scripts["phase11:4:verify"]="node scripts/phase11-4-verify-missing-tool-capability-request-flow.cjs";
  pkg.scripts["tools:capabilities:verify"]="node scripts/phase11-4-verify-missing-tool-capability-request-flow.cjs";
  write(file, JSON.stringify(pkg,null,2)+"\n");
  console.log("OK package.json: Phase 11.4 Scripts eingetragen.");
}
const rootHelper = `import { mkdirSync, readFileSync, writeFileSync } from "node:fs";\nimport path from "node:path";\n\nexport type CapabilityRequestStatus = "pending" | "approved" | "denied" | "implemented" | "expired";\n\nexport interface AgentFlowCapabilityRequest {\n  id: string;\n  status: CapabilityRequestStatus;\n  requestedCapability: string;\n  reason?: string;\n  userInputPreview?: string;\n  riskLevel?: "low" | "medium" | "high";\n  source: "agent-flow";\n  requestedAt: string;\n  decidedAt?: string;\n  decisionNote?: string;\n  metadata?: Record<string, unknown>;\n}\n\nfunction getDataDir(): string {\n  return process.env.TOOL_CONSENT_DATA_DIR || process.env.DATA_DIR || path.join(process.cwd(), "data");\n}\nfunction getFilePath(): string {\n  return path.join(getDataDir(), "tool-capability-requests.json");\n}\nfunction ensureStore(): void {\n  mkdirSync(getDataDir(), { recursive: true });\n  try { readFileSync(getFilePath(), "utf8"); } catch { writeFileSync(getFilePath(), "[]\\n", "utf8"); }\n}\nfunction sanitizePreview(value: string | undefined): string | undefined {\n  if (!value) return undefined;\n  return value\n    .replace(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\\.[A-Z]{2,}/gi, "[redacted-email]")\n    .replace(/(api[_-]?key|token|secret|password)\\s*[:=]\\s*[^\\s,;]+/gi, "$1=[redacted]")\n    .slice(0, 240);\n}\nfunction readRequests(): AgentFlowCapabilityRequest[] {\n  ensureStore();\n  try {\n    const parsed = JSON.parse(readFileSync(getFilePath(), "utf8")) as unknown;\n    return Array.isArray(parsed) ? parsed.filter((entry): entry is AgentFlowCapabilityRequest => Boolean(entry && typeof entry === "object" && typeof (entry as AgentFlowCapabilityRequest).id === "string")) : [];\n  } catch { return []; }\n}\nfunction writeRequests(requests: AgentFlowCapabilityRequest[]): void {\n  ensureStore();\n  writeFileSync(getFilePath(), JSON.stringify(requests, null, 2) + "\\n", "utf8");\n}\nfunction buildId(capability: string, timestamp: string): string {\n  const slug = capability.toLowerCase().replace(/[^a-z0-9äöüß_-]+/g, "-").replace(/^-|-$/g, "").slice(0, 48) || "capability";\n  const compactTime = timestamp.replace(/[^0-9]/g, "").slice(0, 14);\n  const random = Math.random().toString(36).slice(2, 8);\n  return \`capability-\${slug}-\${compactTime}-\${random}\`;\n}\nexport function inferMissingCapabilityRequest(userInput: string, candidateToolIds: string[] = []): { shouldCreate: boolean; requestedCapability: string; reason: string; riskLevel: "low" | "medium" | "high" } {\n  const compact = userInput.toLowerCase();\n  const mentionsNeed = /(tool|werkzeug|fähigkeit|capability|agent|sub-agent|bauen|erstellen|andocken|outlook|kalender|market|marktdaten|broker|portfolio|datei|excel|pdf|mail|email)/i.test(userInput);\n  const explicitMissing = /(fehlt|nicht vorhanden|nicht verfügbar|brauch(e|st)?|benötig(e|st)?|kannst du.*bauen|baue.*agent|erstelle.*tool|missing)/i.test(userInput);\n  if (candidateToolIds.length > 0 || !mentionsNeed || !explicitMissing) {\n    return { shouldCreate: false, requestedCapability: "", reason: "Kein expliziter Missing-Capability-Fall erkannt.", riskLevel: "low" };\n  }\n  const requestedCapability = compact.includes("outlook") || compact.includes("mail") || compact.includes("email")\n    ? "outlook-mail-calendar-capability"\n    : compact.includes("market") || compact.includes("marktdaten") || compact.includes("portfolio") || compact.includes("broker")\n      ? "market-portfolio-data-capability"\n      : compact.includes("agent")\n        ? "agent-blueprint-capability"\n        : "custom-tool-capability";\n  const riskLevel = /outlook|mail|email|broker|portfolio|schreiben|löschen|senden/i.test(userInput) ? "high" : "medium";\n  return {\n    shouldCreate: true,\n    requestedCapability,\n    riskLevel,\n    reason: "Agent Flow erkennt eine explizit angefragte, aber nicht verfügbare Fähigkeit. Es wird nur ein Request erstellt; es wird kein Tool und kein Agent automatisch gebaut.",\n  };\n}\nexport function createAgentFlowCapabilityRequest(input: { requestedCapability: string; reason?: string; userInputPreview?: string; riskLevel?: "low" | "medium" | "high"; metadata?: Record<string, unknown>; }): AgentFlowCapabilityRequest {\n  const requestedAt = new Date().toISOString();\n  const requestedCapability = input.requestedCapability.trim() || "custom-tool-capability";\n  const request: AgentFlowCapabilityRequest = {\n    id: buildId(requestedCapability, requestedAt),\n    status: "pending",\n    requestedCapability,\n    reason: input.reason || "Agent Flow fordert eine fehlende Fähigkeit an.",\n    userInputPreview: sanitizePreview(input.userInputPreview),\n    riskLevel: input.riskLevel || "medium",\n    source: "agent-flow",\n    requestedAt,\n    metadata: { ...(input.metadata || {}), source: "agent-flow" },\n  };\n  writeRequests([request, ...readRequests()]);\n  return request;\n}\n`;
const frontendStore = `import { mkdirSync, readFileSync, writeFileSync } from "node:fs";\nimport path from "node:path";\nexport type CapabilityRequestStatus = "pending" | "approved" | "denied" | "implemented" | "expired";\nexport interface CapabilityRequest { id: string; status: CapabilityRequestStatus; requestedCapability: string; reason?: string; userInputPreview?: string; riskLevel?: "low" | "medium" | "high"; source?: string; requestedAt: string; decidedAt?: string; decisionNote?: string; metadata?: Record<string, unknown>; }\nfunction dataDir(){ return process.env.TOOL_CONSENT_DATA_DIR || process.env.DATA_DIR || path.join(process.cwd(), "..", "data"); }\nfunction filePath(){ return path.join(dataDir(), "tool-capability-requests.json"); }\nfunction ensureStore(){ mkdirSync(dataDir(), { recursive: true }); try { readFileSync(filePath(), "utf8"); } catch { writeFileSync(filePath(), "[]\\n", "utf8"); } }\nfunction sanitize(value: unknown, max=240){ return typeof value === "string" ? value.replace(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\\.[A-Z]{2,}/gi, "[redacted-email]").replace(/(api[_-]?key|token|secret|password)\\s*[:=]\\s*[^\\s,;]+/gi, "$1=[redacted]").slice(0,max) : undefined; }\nfunction readRequests(): CapabilityRequest[]{ ensureStore(); try { const parsed = JSON.parse(readFileSync(filePath(), "utf8")); return Array.isArray(parsed) ? parsed.filter((entry: any) => entry && typeof entry.id === "string") : []; } catch { return []; } }\nfunction writeRequests(requests: CapabilityRequest[]){ ensureStore(); writeFileSync(filePath(), JSON.stringify(requests, null, 2) + "\\n", "utf8"); }\nfunction idFor(capability: string){ const now = new Date().toISOString(); const slug = capability.toLowerCase().replace(/[^a-z0-9äöüß_-]+/g, "-").replace(/^-|-$/g, "").slice(0,48) || "capability"; return \`capability-\${slug}-\${now.replace(/[^0-9]/g, "").slice(0,14)}-\${Math.random().toString(36).slice(2,8)}\`; }\nexport function listCapabilityRequests(){ const requests = readRequests().sort((a,b)=>String(b.requestedAt).localeCompare(String(a.requestedAt))); const counts = requests.reduce((acc, r) => { acc.total++; acc[r.status] = (acc[r.status] || 0) + 1; return acc; }, { total: 0, pending: 0, approved: 0, denied: 0, implemented: 0, expired: 0 } as Record<string, number>); return { ok: true, counts, requests }; }\nexport function createCapabilityRequest(input: { requestedCapability: string; reason?: string; userInputPreview?: string; riskLevel?: "low"|"medium"|"high"; metadata?: Record<string, unknown>; }){ const now = new Date().toISOString(); const requestedCapability = input.requestedCapability.trim(); if(!requestedCapability) throw new Error("requestedCapability ist erforderlich."); const request: CapabilityRequest = { id: idFor(requestedCapability), status: "pending", requestedCapability, reason: sanitize(input.reason, 500), userInputPreview: sanitize(input.userInputPreview), riskLevel: input.riskLevel || "medium", source: "manual-ui", requestedAt: now, metadata: input.metadata }; writeRequests([request, ...readRequests()]); return request; }\nexport function decideCapabilityRequest(input: { id: string; status: "approved"|"denied"|"implemented"; decisionNote?: string }){ const requests = readRequests(); const index = requests.findIndex((r)=>r.id===input.id); if(index < 0) return null; requests[index] = { ...requests[index], status: input.status, decidedAt: new Date().toISOString(), decisionNote: sanitize(input.decisionNote, 500) }; writeRequests(requests); return requests[index]; }\n`;
const apiRoute = `import { createCapabilityRequest, decideCapabilityRequest, listCapabilityRequests } from "../../../lib/tool-capability-request-store";\nexport const dynamic = "force-dynamic";\nexport async function GET(){ try { return Response.json(listCapabilityRequests()); } catch(error){ const message = error instanceof Error ? error.message : "Capability Requests konnten nicht gelesen werden."; return Response.json({ ok:false, error: message }, { status: 500 }); } }\nexport async function POST(request: Request){ try { const body = await request.json(); const requestedCapability = typeof body.requestedCapability === "string" ? body.requestedCapability : ""; const created = createCapabilityRequest({ requestedCapability, reason: typeof body.reason === "string" ? body.reason : undefined, userInputPreview: typeof body.userInputPreview === "string" ? body.userInputPreview : undefined, riskLevel: body.riskLevel === "low" || body.riskLevel === "medium" || body.riskLevel === "high" ? body.riskLevel : undefined, metadata: body.metadata && typeof body.metadata === "object" ? body.metadata : undefined }); return Response.json({ ok:true, request: created }); } catch(error){ const message = error instanceof Error ? error.message : "Capability Request konnte nicht erstellt werden."; return Response.json({ ok:false, error: message }, { status: 400 }); } }\nexport async function PATCH(request: Request){ try { const body = await request.json(); const id = typeof body.id === "string" ? body.id : ""; const status = body.status === "approved" || body.status === "denied" || body.status === "implemented" ? body.status : null; if(!id || !status) return Response.json({ ok:false, error: "id und status approved/denied/implemented sind erforderlich." }, { status: 400 }); const updated = decideCapabilityRequest({ id, status, decisionNote: typeof body.decisionNote === "string" ? body.decisionNote : undefined }); if(!updated) return Response.json({ ok:false, error: "Capability Request nicht gefunden." }, { status: 404 }); return Response.json({ ok:true, request: updated }); } catch(error){ const message = error instanceof Error ? error.message : "Capability Request konnte nicht entschieden werden."; return Response.json({ ok:false, error: message }, { status: 500 }); } }\n`;
const componentPanel = `"use client";\ntype Props = { response: unknown };\nfunction asRecord(value: unknown): Record<string, any> | null { return typeof value === "object" && value !== null ? value as Record<string, any> : null; }\nexport function MissingCapabilityRequestPanel({ response }: Props){\n  const root = asRecord(response); const result = asRecord(root?.result); const request = asRecord(result?.capabilityRequest);\n  const id = result?.capabilityRequestId || request?.id; const url = result?.capabilityUrl || request?.url; const capability = result?.requestedCapability || request?.requestedCapability; const status = result?.status || request?.status;\n  if(!id && !capability) return null;\n  return <section className="panel-card" style={{ borderColor: "#38bdf8", background: "#f0f9ff" }}>\n    <h2 style={{ marginTop: 0 }}>Missing Tool / Capability Request</h2>\n    <p style={{ margin: "8px 0", lineHeight: 1.55 }}>Der Agent hat keine passende bestehende Fähigkeit gefunden und deshalb nur einen kontrollierten Request erstellt. Es wurde kein Tool und kein Agent automatisch gebaut.</p>\n    <ul style={{ margin: "8px 0 12px", paddingLeft: 20 }}>\n      {capability ? <li><strong>Capability:</strong> {String(capability)}</li> : null}\n      {id ? <li><strong>Request ID:</strong> <code>{String(id)}</code></li> : null}\n      {status ? <li><strong>Status:</strong> {String(status)}</li> : null}\n    </ul>\n    {url ? <a className="nav-link" href={String(url)}>Capability Request öffnen</a> : null}\n  </section>;\n}\n`;
const page = `"use client";\nimport { useEffect, useState } from "react";\ntype Req = { id:string; status:string; requestedCapability:string; reason?:string; userInputPreview?:string; riskLevel?:string; requestedAt:string; decisionNote?:string };\nexport default function CapabilityRequestsPage(){\n  const [requests,setRequests]=useState<Req[]>([]); const [error,setError]=useState<string|null>(null); const [cap,setCap]=useState("custom-tool-capability"); const [reason,setReason]=useState("Fehlende Fähigkeit für Agent Flow prüfen.");\n  async function load(){ setError(null); try{ const res=await fetch("/api/capability-requests", { cache:"no-store" }); const payload=await res.json(); if(!res.ok) throw new Error(payload?.error||"Fehler"); setRequests(Array.isArray(payload.requests)?payload.requests:[]); } catch(e){ setError(e instanceof Error ? e.message : "Unbekannter Fehler"); } }\n  useEffect(()=>{ load(); },[]);\n  async function create(){ const res=await fetch("/api/capability-requests", { method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify({ requestedCapability: cap, reason, riskLevel:"medium" }) }); if(!res.ok){ const p=await res.json(); setError(p?.error||"Create fehlgeschlagen"); } await load(); }\n  async function decide(id:string,status:"approved"|"denied"|"implemented"){ const res=await fetch("/api/capability-requests", { method:"PATCH", headers:{"Content-Type":"application/json"}, body: JSON.stringify({ id, status, decisionNote: "UI decision: "+status }) }); if(!res.ok){ const p=await res.json(); setError(p?.error||"Decision fehlgeschlagen"); } await load(); }\n  return <main className="page-wrap"><nav style={{ display:"flex", gap:12, flexWrap:"wrap", marginBottom:18 }}><a className="nav-link" href="/">Chat</a><a className="nav-link" href="/tool-consent">Tool Consent</a><a className="nav-link" href="/capability-requests">Capability Requests</a><a className="nav-link" href="/analytics">Analytics</a><a className="nav-link" href="/system">System</a></nav><div className="page-shell"><section className="hero-card" style={{ background:"linear-gradient(135deg,#e0f2fe 0%,#f8fafc 100%)", borderColor:"#7dd3fc" }}><h1 className="section-title">Missing Tool / Capability Requests</h1><p style={{ lineHeight:1.6 }}>Hier landen kontrollierte Requests, wenn der Agent eine fehlende Fähigkeit erkennt. Approval bedeutet nur Freigabe zur Analyse/Planung, nicht automatische Tool- oder Agent-Erzeugung.</p></section>{error ? <section className="panel-card" style={{ borderColor:"#fecaca", background:"#fef2f2" }}>{error}</section> : null}<section className="panel-card"><h2>Test Request erstellen</h2><input className="text-input" value={cap} onChange={(e)=>setCap(e.target.value)} /><textarea className="text-area" value={reason} onChange={(e)=>setReason(e.target.value)} /><button className="primary-button" type="button" onClick={create}>Capability Request erstellen</button></section><section className="panel-card"><h2>Requests</h2>{requests.length===0 ? <p>Noch keine Capability Requests.</p> : requests.map((r)=><article key={r.id} style={{ borderTop:"1px solid #e5e7eb", padding:"12px 0" }}><div><strong>{r.requestedCapability}</strong> <span className="chip">{r.status}</span> {r.riskLevel ? <span className="chip">{r.riskLevel}</span> : null}</div><div className="helper-text"><code>{r.id}</code> · {r.requestedAt}</div>{r.reason ? <p>{r.reason}</p> : null}{r.userInputPreview ? <pre style={{ whiteSpace:"pre-wrap" }}>{r.userInputPreview}</pre> : null}{r.status === "pending" ? <div style={{ display:"flex", gap:8, flexWrap:"wrap" }}><button className="secondary-button" onClick={()=>decide(r.id,"approved")}>Approve</button><button className="secondary-button" onClick={()=>decide(r.id,"denied")}>Deny</button><button className="secondary-button" onClick={()=>decide(r.id,"implemented")}>Implemented</button></div> : null}</article>)}</section></div></main>;\n}\n`;
function patchDockerfile(){ const file="Dockerfile"; if(!exists(file)) return; let content=read(file); if(!content.includes("COPY tool-capability-request-agent-flow.ts ./")){ const marker="COPY tool-consent-agent-flow.ts ./\n"; content = content.includes(marker) ? content.replace(marker, marker+"COPY tool-capability-request-agent-flow.ts ./\n") : content + "\nCOPY tool-capability-request-agent-flow.ts ./\n"; write(file, content); console.log("OK Dockerfile: Capability Helper aufgenommen."); } else console.log("SKIP Dockerfile: Capability Helper bereits vorhanden."); }
function patchServer(){
  const file="server.ts"; let content=read(file); const original=content;
  const importLine='import { createAgentFlowCapabilityRequest, inferMissingCapabilityRequest } from "./tool-capability-request-agent-flow";';
  if(!content.includes(importLine)){ const marker1='import { createAgentFlowToolConsentRequest, getAgentFlowToolConsentRequest } from "./tool-consent-agent-flow";'; const marker2='import { createAgentFlowToolConsentRequest } from "./tool-consent-agent-flow";'; if(content.includes(marker1)) content=content.replace(marker1, marker1+"\n"+importLine); else if(content.includes(marker2)) content=content.replace(marker2, marker2+"\n"+importLine); else content=content.replace(/import \{ createAgentFlowToolConsentRequest[^\n]+\n/, m=>m+importLine+"\n"); }
  if(!content.includes("PHASE 11.4: Missing Tool / Capability Request Flow")){
    const marker="  // PHASE 11.3: Approved Consent Resume Gate\n";
    if(!content.includes(marker)) throw new Error("Phase 11.3 Marker in server.ts nicht gefunden.");
    const block=`  // PHASE 11.4: Missing Tool / Capability Request Flow\n  const missingCapability = inferMissingCapabilityRequest(effectiveUserInput, toolPreflight.candidateToolIds ?? []);\n  if (missingCapability.shouldCreate) {\n    const capabilityRequest = createAgentFlowCapabilityRequest({\n      requestedCapability: missingCapability.requestedCapability,\n      reason: missingCapability.reason,\n      userInputPreview: body.userInput,\n      riskLevel: missingCapability.riskLevel,\n      metadata: { source: "agent-flow", toolPreflight, toolEnforcement },\n    });\n    const capabilityUrl = "/capability-requests?requestId=" + encodeURIComponent(capabilityRequest.id);\n    const payload = {\n      ok: true,\n      mode: "cloud",\n      sensitivity: body.sensitivity ?? "internal",\n      processingMode: body.processingMode ?? "auto",\n      processingPath,\n      redacted: processingPath === "cloud_redacted",\n      result: {\n        answer: "Für diese Anfrage fehlt eine passende Tool- oder Agent-Fähigkeit. Es wurde ein kontrollierter Capability Request erstellt; es wird nichts automatisch gebaut oder ausgeführt.",\n        capabilityRequestCreated: true,\n        capabilityRequestId: capabilityRequest.id,\n        capabilityUrl,\n        requestedCapability: capabilityRequest.requestedCapability,\n        status: capabilityRequest.status,\n        riskLevel: capabilityRequest.riskLevel,\n        toolPreflight,\n        toolEnforcement,\n        capabilityRequest: {\n          id: capabilityRequest.id,\n          url: capabilityUrl,\n          requestedCapability: capabilityRequest.requestedCapability,\n          status: capabilityRequest.status,\n          riskLevel: capabilityRequest.riskLevel,\n          source: "agent-flow",\n        },\n      },\n    };\n    await appendDecisionLog({\n      timestamp: new Date().toISOString(),\n      route: "direct",\n      userInput: body.userInput,\n      recommendation: null,\n      firstStep: "Capability Request erstellt: " + capabilityUrl,\n      confidence: null,\n      context: body.context ?? [],\n      extractedOptions: [],\n      suggestedAgents: ["privacy_agent"],\n      routingSummary: "Missing Capability Request | Agent Flow | " + capabilityRequest.requestedCapability,\n      routingDetails: undefined,\n      toolPreflight,\n      toolEnforcement,\n      capabilityRequestCreated: true,\n      capabilityRequestId: capabilityRequest.id,\n      capabilityUrl,\n    } as any);\n    sendJson(res, 200, payload);\n    return;\n  }\n\n`;
    content=content.replace(marker, block+marker);
  }
  if(content!==original){ write(file, content); console.log("OK server.ts: Missing Capability Flow ergänzt."); } else console.log("SKIP server.ts: Missing Capability Flow bereits vorhanden.");
}
function patchFrontend(){
  ensureFile("frontend/lib/tool-capability-request-store.ts", frontendStore);
  ensureFile("frontend/app/api/capability-requests/route.ts", apiRoute);
  ensureFile("frontend/components/MissingCapabilityRequestPanel.tsx", componentPanel);
  ensureFile("frontend/app/capability-requests/page.tsx", page);
  const file="frontend/app/page.tsx"; if(!exists(file)) return;
  let content=read(file); const original=content;
  const imp='import { MissingCapabilityRequestPanel } from "../components/MissingCapabilityRequestPanel";';
  if(!content.includes(imp)){ const lines=content.split(/\r?\n/); let last=-1; for(let i=0;i<lines.length;i++) if(lines[i].startsWith("import ")) last=i; if(last>=0){ lines.splice(last+1,0,imp); content=lines.join("\n"); } }
  if(!content.includes("<MissingCapabilityRequestPanel response={response} />")){
    const line="                <MissingCapabilityRequestPanel response={response} />";
    if(content.includes("<AgentFlowConsentRequestPanel response={response} />")) content=content.replace(/(\s*<AgentFlowConsentRequestPanel response=\{response\} \/>\s*)/, `$1${line}\n`);
    else { const mainClose=content.lastIndexOf("</main>"); if(mainClose>=0) content=content.slice(0,mainClose)+line+"\n"+content.slice(mainClose); }
  }
  if(!content.includes('href="/capability-requests"')){
    if(content.includes('href="/tool-consent"')) content=content.replace(/(<a className="nav-link" href="\/tool-consent">[^<]+<\/a>)/, '$1\n        <a className="nav-link" href="/capability-requests">Capability Requests</a>');
  }
  if(content!==original){ write(file, content); console.log("OK frontend/app/page.tsx: MissingCapabilityRequestPanel/Navigation ergänzt."); } else console.log("SKIP frontend/app/page.tsx: bereits vorbereitet.");
}
function patchDocs(){
  ensureFile("phase11-4-missing-tool-capability-request-flow.md", `# Phase 11.4 – Missing Tool / Capability Request Flow\n\n## Ziel\nDer Agent darf fehlende Tools oder Fähigkeiten erkennen und daraus einen kontrollierten Capability Request erstellen. Er baut aber noch keine Tools und aktiviert keine neuen Agenten automatisch.\n\n## Flow\n1. Agent Flow erkennt explizit angefragte, aber fehlende Fähigkeit.\n2. Es wird ein Request mit Status \`pending\` erstellt.\n3. Response enthält \`capabilityRequestId\` und \`capabilityUrl\`.\n4. UI zeigt Link auf \`/capability-requests?requestId=<id>\`.\n5. Admin/User kann approved, denied oder implemented setzen.\n\n## Grenze\nAgent Blueprint Proposal und echte Agent Factory kommen später.\n`);
  ensureFile("docs/phase11-missing-tool-capability-request-flow-runbook.md", `# Runbook – Phase 11.4 Missing Tool / Capability Request Flow\n\n## Patch\n\`\`\`powershell\nnpm run phase11:4:patch\n\`\`\`\n\nFalls Script noch nicht registriert ist:\n\`\`\`powershell\nnode scripts/phase11-4-patch-missing-tool-capability-request-flow.cjs\n\`\`\`\n\n## Verify\n\`\`\`powershell\nnpm run phase11:4:verify\nnpm run build\nnpm run stack:health\n\`\`\`\n\n## Manuelle Prüfung\n1. Chat öffnen.\n2. Explizit fehlende Fähigkeit anfordern, z.B. \"Ich brauche ein Outlook Tool, das Mails und Kalender integrieren kann.\"\n3. Erwartung: kein Tool wird gebaut; Response enthält \`capabilityRequestId\` und \`capabilityUrl\`.\n4. \`/capability-requests\` öffnen und Request prüfen/entscheiden.\n`);
}
patchPackage();
ensureFile("tool-capability-request-agent-flow.ts", rootHelper);
patchDockerfile();
patchServer();
patchFrontend();
patchDocs();
console.log("Phase 11.4 Patch abgeschlossen.");
