const fs = require("fs");
const path = require("path");
function full(file){ return path.join(process.cwd(), file); }
function exists(file){ return fs.existsSync(full(file)); }
function read(file){ return exists(file) ? fs.readFileSync(full(file), "utf8") : ""; }
function write(file, content){ fs.mkdirSync(path.dirname(full(file)), { recursive: true }); fs.writeFileSync(full(file), content, "utf8"); console.log("OK " + file); }

write("frontend/lib/provider-dispatch-approval-policy-confirmation-envelope-store.ts", "import { mkdirSync, readFileSync, appendFileSync } from \"node:fs\";\nimport path from \"node:path\";\nimport { appendGovernanceAuditEvent } from \"./governance-audit-store\";\n\nexport type ProviderDispatchApprovalPolicyConfirmationDecision =\n  | \"provider_dispatch_approval_policy_confirmation_envelope_prepared_no_provider_call\"\n  | \"blocked_missing_approval_candidate_policy_simulation\"\n  | \"blocked_policy_did_not_allow_human_approval_only\"\n  | \"blocked_approval_candidate_not_human_approval_ready\"\n  | \"blocked_approval_candidate_approved\"\n  | \"blocked_approval_candidate_executed\"\n  | \"blocked_approval_candidate_contains_provider_response\"\n  | \"blocked_approval_candidate_contains_prompt_payload\"\n  | \"blocked_approval_candidate_contains_secrets\"\n  | \"blocked_execution_or_dispatch_enabled\"\n  | \"blocked_network_or_provider_execution_attempt\";\n\nexport interface ProviderDispatchApprovalPolicyConfirmationEnvelope {\n  id: string;\n  timestamp: string;\n  providerDispatchApprovalCandidateEnvelopePolicySimulationId?: string;\n  providerDispatchApprovalCandidateEnvelopeId?: string;\n  providerDispatchReleaseCandidateEnvelopeId?: string;\n  decision: ProviderDispatchApprovalPolicyConfirmationDecision;\n  envelopeMode: \"controlled_provider_dispatch_approval_policy_confirmation_envelope_no_provider_call\";\n  providerDispatchApprovalPolicyConfirmationEnvelopePrepared: true;\n  approvalPolicyConfirmationEnvelopePrepared: true;\n  approvalPolicyConfirmationEnvelopePersisted: true;\n  approvalPolicyConfirmedForHumanApprovalOnly: true;\n  approvalCandidateReadyForHumanApproval: true;\n  approvalCandidateApproved: false;\n  approvalCandidateExecuted: false;\n  approvalCandidateContainsProviderResponse: false;\n  approvalCandidateContainsPromptPayload: false;\n  approvalCandidateContainsSecrets: false;\n  releaseCandidateReadyForHumanReview: true;\n  releaseCandidateApproved: false;\n  releaseCandidateExecuted: false;\n  finalDispatchAllowed: false;\n  providerDispatchPerformed: false;\n  commandEnvelopeExecuted: false;\n  executionGateOpen: false;\n  metadataOnly: true;\n  provider: \"none\";\n  modelSelected: \"none\";\n  promptPayloadIncluded: false;\n  promptIncluded: false;\n  providerResponseIncluded: false;\n  providerResultIncluded: false;\n  secretValuesIncluded: false;\n  requestBodyIncluded: false;\n  sensitiveRequestBodyIncluded: false;\n  networkCallAllowed: false;\n  networkCallPerformed: false;\n  providerExecutionAllowed: false;\n  realLlmCallAllowed: false;\n  llmCallPerformed: false;\n  executionAllowed: false;\n  toolExecutionAllowed: false;\n  agentExecutionAllowed: false;\n  dryRunOnly: true;\n  noSecretsIncluded: boolean;\n  reason: string;\n  metadata?: Record<string, unknown>;\n}\n\nfunction dataDir(): string {\n  return process.env.TOOL_CONSENT_DATA_DIR || process.env.DATA_DIR || path.join(process.cwd(), \"..\", \"data\");\n}\nfunction policySimulationPath(): string {\n  return path.join(dataDir(), \"provider-dispatch-approval-candidate-envelope-policy-simulations.jsonl\");\n}\nfunction confirmationPath(): string {\n  return path.join(dataDir(), \"provider-dispatch-approval-policy-confirmation-envelopes.jsonl\");\n}\nfunction ensureStore(): void { mkdirSync(dataDir(), { recursive: true }); }\nfunction readJsonl(file: string): any[] {\n  try {\n    return readFileSync(file, \"utf8\").split(\"\\n\").map((line) => line.trim()).filter(Boolean).map((line) => {\n      try { return JSON.parse(line); } catch { return null; }\n    }).filter(Boolean);\n  } catch { return []; }\n}\nfunction makeId(prefix: string): string {\n  return prefix + \"-\" + new Date().toISOString().replace(/[^0-9]/g, \"\").slice(0, 14) + \"-\" + Math.random().toString(36).slice(2, 8);\n}\nfunction appendEnvelope(envelope: ProviderDispatchApprovalPolicyConfirmationEnvelope): void {\n  ensureStore();\n  appendFileSync(confirmationPath(), JSON.stringify(envelope) + \"\\n\", \"utf8\");\n}\nfunction containsSecretValue(value: unknown): boolean {\n  return /(sk-[a-z0-9_-]{10,}|api[_-]?key\\s*[:=]\\s*[^\\s,;]+|token\\s*[:=]\\s*[^\\s,;]+|secret\\s*[:=]\\s*[^\\s,;]+|password\\s*[:=]\\s*[^\\s,;]+)/i.test(JSON.stringify(value || {}));\n}\n\nexport function listProviderDispatchApprovalPolicyConfirmationEnvelopes(limit = 100): ProviderDispatchApprovalPolicyConfirmationEnvelope[] {\n  ensureStore();\n  return readJsonl(confirmationPath()).sort((a, b) => String(b.timestamp).localeCompare(String(a.timestamp))).slice(0, Math.max(1, Math.min(limit, 500)));\n}\n\nexport function createProviderDispatchApprovalPolicyConfirmationEnvelope(input: { providerDispatchApprovalCandidateEnvelopePolicySimulationId?: string; metadata?: Record<string, unknown> }): ProviderDispatchApprovalPolicyConfirmationEnvelope {\n  ensureStore();\n  const simulations = readJsonl(policySimulationPath());\n  const simulation = input.providerDispatchApprovalCandidateEnvelopePolicySimulationId\n    ? simulations.find((entry: any) => entry.id === input.providerDispatchApprovalCandidateEnvelopePolicySimulationId)\n    : simulations[0];\n\n  let decision: ProviderDispatchApprovalPolicyConfirmationDecision = \"provider_dispatch_approval_policy_confirmation_envelope_prepared_no_provider_call\";\n  let reason = \"Approval Policy Confirmation Envelope wurde vorbereitet. Es best\u00e4tigt nur Human-Approval-only Policy, ohne Approval, ohne Execution, ohne Provider Dispatch und ohne Provider-/Netzwerk-Aufruf.\";\n\n  if (!simulation) {\n    decision = \"blocked_missing_approval_candidate_policy_simulation\";\n    reason = \"Approval Candidate Policy Simulation fehlt.\";\n  } else if (simulation.decision !== \"provider_dispatch_approval_candidate_envelope_policy_allowed_human_approval_only_no_provider_call\") {\n    decision = \"blocked_policy_did_not_allow_human_approval_only\";\n    reason = \"Policy Simulation erlaubt Human-Approval-only nicht.\";\n  } else if (simulation.approvalCandidateReadyForHumanApproval !== true) {\n    decision = \"blocked_approval_candidate_not_human_approval_ready\";\n    reason = \"Approval Candidate ist nicht Human-Approval-ready.\";\n  } else if (simulation.approvalCandidateApproved !== false) {\n    decision = \"blocked_approval_candidate_approved\";\n    reason = \"Approval Candidate wurde approved.\";\n  } else if (simulation.approvalCandidateExecuted !== false) {\n    decision = \"blocked_approval_candidate_executed\";\n    reason = \"Approval Candidate wurde ausgef\u00fchrt.\";\n  } else if (simulation.approvalCandidateContainsProviderResponse !== false || simulation.providerResponseIncluded !== false || simulation.providerResultIncluded !== false) {\n    decision = \"blocked_approval_candidate_contains_provider_response\";\n    reason = \"Approval Candidate enth\u00e4lt Provider Response oder Provider Result.\";\n  } else if (simulation.approvalCandidateContainsPromptPayload !== false || simulation.promptPayloadIncluded !== false || simulation.promptIncluded !== false) {\n    decision = \"blocked_approval_candidate_contains_prompt_payload\";\n    reason = \"Approval Candidate enth\u00e4lt Prompt Payload.\";\n  } else if (simulation.approvalCandidateContainsSecrets !== false || simulation.secretValuesIncluded !== false || simulation.noSecretsIncluded !== true || containsSecretValue(simulation)) {\n    decision = \"blocked_approval_candidate_contains_secrets\";\n    reason = \"Approval Candidate enth\u00e4lt Secret-Werte.\";\n  } else if (simulation.finalDispatchAllowed !== false || simulation.providerDispatchPerformed !== false || simulation.commandEnvelopeExecuted !== false || simulation.executionGateOpen !== false || simulation.executionAllowed !== false || simulation.toolExecutionAllowed !== false || simulation.agentExecutionAllowed !== false || simulation.dryRunOnly !== true) {\n    decision = \"blocked_execution_or_dispatch_enabled\";\n    reason = \"Dispatch oder Execution ist aktiv.\";\n  } else if (simulation.networkCallAllowed !== false || simulation.networkCallPerformed !== false || simulation.providerExecutionAllowed !== false || simulation.llmCallPerformed !== false) {\n    decision = \"blocked_network_or_provider_execution_attempt\";\n    reason = \"Netzwerk-/Provider-Ausf\u00fchrung erkannt.\";\n  }\n\n  const envelope: ProviderDispatchApprovalPolicyConfirmationEnvelope = {\n    id: makeId(\"provider-dispatch-approval-policy-confirmation-envelope\"),\n    timestamp: new Date().toISOString(),\n    providerDispatchApprovalCandidateEnvelopePolicySimulationId: simulation?.id || input.providerDispatchApprovalCandidateEnvelopePolicySimulationId,\n    providerDispatchApprovalCandidateEnvelopeId: simulation?.providerDispatchApprovalCandidateEnvelopeId,\n    providerDispatchReleaseCandidateEnvelopeId: simulation?.providerDispatchReleaseCandidateEnvelopeId,\n    decision,\n    envelopeMode: \"controlled_provider_dispatch_approval_policy_confirmation_envelope_no_provider_call\",\n    providerDispatchApprovalPolicyConfirmationEnvelopePrepared: true,\n    approvalPolicyConfirmationEnvelopePrepared: true,\n    approvalPolicyConfirmationEnvelopePersisted: true,\n    approvalPolicyConfirmedForHumanApprovalOnly: true,\n    approvalCandidateReadyForHumanApproval: true,\n    approvalCandidateApproved: false,\n    approvalCandidateExecuted: false,\n    approvalCandidateContainsProviderResponse: false,\n    approvalCandidateContainsPromptPayload: false,\n    approvalCandidateContainsSecrets: false,\n    releaseCandidateReadyForHumanReview: true,\n    releaseCandidateApproved: false,\n    releaseCandidateExecuted: false,\n    finalDispatchAllowed: false,\n    providerDispatchPerformed: false,\n    commandEnvelopeExecuted: false,\n    executionGateOpen: false,\n    metadataOnly: true,\n    provider: \"none\",\n    modelSelected: \"none\",\n    promptPayloadIncluded: false,\n    promptIncluded: false,\n    providerResponseIncluded: false,\n    providerResultIncluded: false,\n    secretValuesIncluded: false,\n    requestBodyIncluded: false,\n    sensitiveRequestBodyIncluded: false,\n    networkCallAllowed: false,\n    networkCallPerformed: false,\n    providerExecutionAllowed: false,\n    realLlmCallAllowed: false,\n    llmCallPerformed: false,\n    executionAllowed: false,\n    toolExecutionAllowed: false,\n    agentExecutionAllowed: false,\n    dryRunOnly: true,\n    noSecretsIncluded: decision !== \"blocked_approval_candidate_contains_secrets\",\n    reason,\n    metadata: { ...(input.metadata || {}), phase: \"42.0\", noProviderCall: true, noNetworkCall: true, noDispatch: true, approvalPolicyConfirmedForHumanApprovalOnly: true, approvalCandidateApproved: false, approvalCandidateExecuted: false }\n  };\n\n  appendEnvelope(envelope);\n  appendGovernanceAuditEvent({\n    type: \"agent_registry_status_changed\",\n    actor: \"api\",\n    entityType: \"agent-registry\",\n    entityId: envelope.providerDispatchApprovalCandidateEnvelopePolicySimulationId,\n    status: envelope.decision,\n    riskLevel: \"critical\",\n    summary: \"Provider Dispatch Approval Policy Confirmation Envelope: \" + envelope.decision,\n    metadata: { source: \"phase42.0-provider-dispatch-approval-policy-confirmation-envelope\", envelopeId: envelope.id, approvalPolicyConfirmedForHumanApprovalOnly: true, approvalCandidateApproved: false, approvalCandidateExecuted: false, networkCallAllowed: false, networkCallPerformed: false, providerExecutionAllowed: false, llmCallPerformed: false }\n  });\n  return envelope;\n}\n\nexport function summarizeProviderDispatchApprovalPolicyConfirmationEnvelopes(envelopes: ProviderDispatchApprovalPolicyConfirmationEnvelope[]) {\n  const byDecision: Record<string, number> = {};\n  for (const item of envelopes) byDecision[item.decision] = (byDecision[item.decision] || 0) + 1;\n  return { total: envelopes.length, byDecision };\n}\n");
write("frontend/app/api/provider-dispatch-approval-policy-confirmation-envelope/route.ts", "import { createProviderDispatchApprovalPolicyConfirmationEnvelope, listProviderDispatchApprovalPolicyConfirmationEnvelopes, summarizeProviderDispatchApprovalPolicyConfirmationEnvelopes } from \"../../../lib/provider-dispatch-approval-policy-confirmation-envelope-store\";\nexport const dynamic = \"force-dynamic\";\nexport async function GET(request: Request) {\n  try {\n    const url = new URL(request.url);\n    const limit = Number(url.searchParams.get(\"limit\") || \"100\");\n    const providerDispatchApprovalPolicyConfirmationEnvelopes = listProviderDispatchApprovalPolicyConfirmationEnvelopes(Number.isFinite(limit) ? limit : 100);\n    return Response.json({ ok: true, summary: summarizeProviderDispatchApprovalPolicyConfirmationEnvelopes(providerDispatchApprovalPolicyConfirmationEnvelopes), providerDispatchApprovalPolicyConfirmationEnvelopes });\n  } catch (error) {\n    const message = error instanceof Error ? error.message : \"Provider Dispatch Approval Policy Confirmation Envelopes konnten nicht gelesen werden.\";\n    return Response.json({ ok: false, error: message }, { status: 500 });\n  }\n}\nexport async function POST(request: Request) {\n  try {\n    const body = await request.json();\n    const envelope = createProviderDispatchApprovalPolicyConfirmationEnvelope({ providerDispatchApprovalCandidateEnvelopePolicySimulationId: typeof body.providerDispatchApprovalCandidateEnvelopePolicySimulationId === \"string\" ? body.providerDispatchApprovalCandidateEnvelopePolicySimulationId : undefined, metadata: body.metadata && typeof body.metadata === \"object\" ? body.metadata : undefined });\n    return Response.json({ ok: true, envelope });\n  } catch (error) {\n    const message = error instanceof Error ? error.message : \"Provider Dispatch Approval Policy Confirmation Envelope konnte nicht erstellt werden.\";\n    return Response.json({ ok: false, error: message }, { status: 400 });\n  }\n}\n");
write("frontend/app/provider-dispatch-approval-policy-confirmation-envelope/page.tsx", "\"use client\";\nimport { useEffect, useState } from \"react\";\nimport { UnifiedNavigation } from \"../../components/UnifiedNavigation\";\n\ntype Simulation = { id: string; decision: string; timestamp: string; policyMode: string };\ntype Confirmation = { id: string; timestamp: string; decision: string; reason: string; envelopeMode: string; providerDispatchApprovalPolicyConfirmationEnvelopePrepared: boolean; approvalPolicyConfirmedForHumanApprovalOnly: boolean; approvalCandidateApproved: boolean; approvalCandidateExecuted: boolean; networkCallPerformed: boolean; providerExecutionAllowed: boolean; llmCallPerformed: boolean; dryRunOnly: boolean };\n\nexport default function ProviderDispatchApprovalPolicyConfirmationEnvelopePage() {\n  const [simulations, setSimulations] = useState<Simulation[]>([]);\n  const [confirmations, setConfirmations] = useState<Confirmation[]>([]);\n  const [summary, setSummary] = useState<unknown>(null);\n  const [selected, setSelected] = useState(\"\");\n  const [error, setError] = useState<string | null>(null);\n\n  async function load() {\n    setError(null);\n    try {\n      const [sRes, cRes] = await Promise.all([\n        fetch(\"/api/provider-dispatch-approval-candidate-envelope-policy?limit=100\", { cache: \"no-store\" }),\n        fetch(\"/api/provider-dispatch-approval-policy-confirmation-envelope?limit=100\", { cache: \"no-store\" })\n      ]);\n      const s = await sRes.json();\n      const c = await cRes.json();\n      if (sRes.ok) {\n        const list = Array.isArray(s.simulations) ? s.simulations : [];\n        setSimulations(list);\n        if (!selected && list[0]?.id) setSelected(list[0].id);\n      }\n      if (!cRes.ok) throw new Error(c?.error || \"Policy Confirmation Envelopes konnten nicht geladen werden.\");\n      setConfirmations(Array.isArray(c.providerDispatchApprovalPolicyConfirmationEnvelopes) ? c.providerDispatchApprovalPolicyConfirmationEnvelopes : []);\n      setSummary(c.summary || null);\n    } catch (err) { setError(err instanceof Error ? err.message : \"Unbekannter Fehler\"); }\n  }\n\n  useEffect(() => { load(); }, []);\n\n  async function createEnvelope() {\n    const res = await fetch(\"/api/provider-dispatch-approval-policy-confirmation-envelope\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ providerDispatchApprovalCandidateEnvelopePolicySimulationId: selected }) });\n    if (!res.ok) { const p = await res.json(); setError(p?.error || \"Policy Confirmation Envelope fehlgeschlagen\"); }\n    await load();\n  }\n\n  return <main className=\"page-wrap\"><UnifiedNavigation active=\"provider-dispatch-approval-policy-confirmation-envelope\" /><div className=\"page-shell\"><section className=\"hero-card\"><h1 className=\"section-title\">Provider Dispatch Approval Policy Confirmation Envelope</h1><p style={{ lineHeight: 1.6 }}>Phase 42.0 best\u00e4tigt eine Approval Candidate Policy Simulation als Human-Approval-only Confirmation Envelope. Keine Approval-Ausf\u00fchrung, kein Provider Dispatch und kein Provider-/Netzwerk-Aufruf.</p></section>{error ? <section className=\"panel-card\" style={{ borderColor: \"#fecaca\", background: \"#fef2f2\" }}>{error}</section> : null}<section className=\"panel-card\"><h2>Policy Confirmation Envelope vorbereiten</h2><select className=\"text-input\" value={selected} onChange={(ev) => setSelected(ev.target.value)}>{simulations.map((item) => <option key={item.id} value={item.id}>{item.policyMode} \u00b7 {item.decision} \u00b7 {item.id}</option>)}</select><button className=\"primary-button\" type=\"button\" onClick={createEnvelope} disabled={!selected}>Provider Dispatch Approval Policy Confirmation Envelope vorbereiten</button></section><section className=\"panel-card\"><h2>Summary</h2><pre style={{ whiteSpace: \"pre-wrap\" }}>{JSON.stringify(summary ?? {}, null, 2)}</pre></section><section className=\"panel-card\"><h2>Confirmation Envelopes</h2>{confirmations.length === 0 ? <p>Noch keine Policy Confirmation Envelopes.</p> : confirmations.map((item) => <article key={item.id} style={{ borderTop: \"1px solid #e5e7eb\", padding: \"12px 0\" }}><div><strong>{item.envelopeMode}</strong> <span className=\"chip\">{item.decision}</span></div><div className=\"helper-text\"><code>{item.id}</code> \u00b7 {item.timestamp}</div><p><strong>Reason:</strong> {item.reason}</p><p><strong>Prepared:</strong> {String(item.providerDispatchApprovalPolicyConfirmationEnvelopePrepared)} \u00b7 <strong>Human only:</strong> {String(item.approvalPolicyConfirmedForHumanApprovalOnly)} \u00b7 <strong>Approved:</strong> {String(item.approvalCandidateApproved)} \u00b7 <strong>Executed:</strong> {String(item.approvalCandidateExecuted)}</p><p><strong>Network call:</strong> {String(item.networkCallPerformed)} \u00b7 <strong>Provider execution:</strong> {String(item.providerExecutionAllowed)} \u00b7 <strong>LLM Call:</strong> {String(item.llmCallPerformed)} \u00b7 <strong>Dry-run:</strong> {String(item.dryRunOnly)}</p></article>)}</section></div></main>;\n}\n");
write("phase42-0-provider-dispatch-approval-policy-confirmation-envelope.md", "# Phase 42.0 \u2013 Controlled Provider Dispatch Approval Policy Confirmation Envelope / Still No Provider Call\n\n## Ziel\n\nEin Provider Dispatch Approval Policy Confirmation Envelope best\u00e4tigt nur die Human-Approval-only Policy Simulation aus Phase 41.1. Es bleibt ohne Approval-Ausf\u00fchrung, ohne Provider Dispatch und ohne Provider-/Netzwerk-Aufruf.\n\n## UI/API\n\n- UI: /provider-dispatch-approval-policy-confirmation-envelope\n- API: /api/provider-dispatch-approval-policy-confirmation-envelope\n\n## Sicherheitsinvarianten\n\n- providerDispatchApprovalPolicyConfirmationEnvelopePrepared=true\n- approvalPolicyConfirmationEnvelopePrepared=true\n- approvalPolicyConfirmationEnvelopePersisted=true\n- approvalPolicyConfirmedForHumanApprovalOnly=true\n- approvalCandidateReadyForHumanApproval=true\n- approvalCandidateApproved=false\n- approvalCandidateExecuted=false\n- approvalCandidateContainsProviderResponse=false\n- approvalCandidateContainsPromptPayload=false\n- approvalCandidateContainsSecrets=false\n- networkCallAllowed=false\n- networkCallPerformed=false\n- providerExecutionAllowed=false\n- realLlmCallAllowed=false\n- llmCallPerformed=false\n- executionAllowed=false\n- toolExecutionAllowed=false\n- agentExecutionAllowed=false\n- dryRunOnly=true\n\n## N\u00e4chster Schritt\n\nPhase 42.1 \u2013 Provider Dispatch Approval Policy Confirmation Policy & Audit\n");
write("docs/phase42-provider-dispatch-approval-policy-confirmation-envelope-runbook.md", "# Runbook \u2013 Phase 42.0 Provider Dispatch Approval Policy Confirmation Envelope\n\n## Patch\n\n```powershell\nnpm run phase42:0:patch\n```\n\n## Verify\n\n```powershell\nnpm run phase42:0:verify\nnpm run build\n```\n\n## Optional Browser\n\n```text\nhttp://localhost:3000/provider-dispatch-approval-policy-confirmation-envelope\n```\n");
write("scripts/phase42-0-verify-provider-dispatch-approval-policy-confirmation-envelope.cjs", "const fs = require(\"fs\");\nconst path = require(\"path\");\nfunction full(file){ return path.join(process.cwd(), file); }\nfunction exists(file){ return fs.existsSync(full(file)); }\nfunction read(file){ return exists(file) ? fs.readFileSync(full(file), \"utf8\") : \"\"; }\nfunction check(file, patterns){\n  if(!exists(file)){ console.log(\"MISS \" + file); return false; }\n  const content = read(file);\n  let ok = true;\n  for(const p of patterns){\n    const found = content.includes(p);\n    console.log((found ? \"OK  \" : \"MISS\") + \" \" + file + \": \" + p);\n    if(!found) ok = false;\n  }\n  return ok;\n}\nconsole.log(\"======================================\");\nconsole.log(\" Phase 42.0 Approval Policy Confirmation Envelope Verify\");\nconsole.log(\"======================================\");\nlet ok = true;\nok = check(\"frontend/lib/provider-dispatch-approval-policy-confirmation-envelope-store.ts\", [\n  \"createProviderDispatchApprovalPolicyConfirmationEnvelope\",\n  \"controlled_provider_dispatch_approval_policy_confirmation_envelope_no_provider_call\",\n  \"providerDispatchApprovalPolicyConfirmationEnvelopePrepared: true\",\n  \"approvalPolicyConfirmationEnvelopePrepared: true\",\n  \"approvalPolicyConfirmationEnvelopePersisted: true\",\n  \"approvalPolicyConfirmedForHumanApprovalOnly: true\",\n  \"approvalCandidateApproved: false\",\n  \"approvalCandidateExecuted: false\",\n  \"networkCallPerformed: false\",\n  \"providerExecutionAllowed: false\",\n  \"llmCallPerformed: false\",\n  \"dryRunOnly: true\"\n]) && ok;\nok = check(\"frontend/app/api/provider-dispatch-approval-policy-confirmation-envelope/route.ts\", [\"createProviderDispatchApprovalPolicyConfirmationEnvelope\", \"GET\", \"POST\"]) && ok;\nok = check(\"frontend/app/provider-dispatch-approval-policy-confirmation-envelope/page.tsx\", [\n  \"Provider Dispatch Approval Policy Confirmation Envelope\",\n  \"Policy Confirmation Envelope vorbereiten\",\n  \"approvalPolicyConfirmedForHumanApprovalOnly\",\n  \"approvalCandidateApproved\",\n  \"approvalCandidateExecuted\",\n  \"networkCallPerformed\"\n]) && ok;\nok = check(\"frontend/components/UnifiedNavigation.tsx\", [\n  \"/provider-dispatch-approval-policy-confirmation-envelope\",\n  \"Approval Policy Confirmation\",\n  \"provider-dispatch-approval-policy-confirmation-envelope\"\n]) && ok;\nok = check(\"phase42-0-provider-dispatch-approval-policy-confirmation-envelope.md\", [\n  \"Phase 42.0\",\n  \"Phase 42.1\",\n  \"providerDispatchApprovalPolicyConfirmationEnvelopePrepared=true\",\n  \"approvalPolicyConfirmationEnvelopePrepared=true\",\n  \"approvalPolicyConfirmationEnvelopePersisted=true\",\n  \"approvalPolicyConfirmedForHumanApprovalOnly=true\",\n  \"approvalCandidateApproved=false\",\n  \"approvalCandidateExecuted=false\",\n  \"networkCallPerformed=false\",\n  \"providerExecutionAllowed=false\",\n  \"dryRunOnly=true\"\n]) && ok;\nok = check(\"docs/phase42-provider-dispatch-approval-policy-confirmation-envelope-runbook.md\", [\"phase42:0:patch\", \"phase42:0:verify\"]) && ok;\nok = check(\"package.json\", [\"phase42:0:patch\", \"phase42:0:verify\", \"llm:provider-dispatch-approval-policy-confirmation-envelope:verify\"]) && ok;\nif(!ok){ console.error(\"Verify fehlgeschlagen.\"); process.exit(1); }\nconsole.log(\"Verify OK. Phase 42.0 Provider Dispatch Approval Policy Confirmation Envelope ist vorbereitet.\");\n");

const navFile = "frontend/components/UnifiedNavigation.tsx";
let nav = read(navFile);
if (nav && !nav.includes("provider-dispatch-approval-policy-confirmation-envelope")) {
  const route = '  { href: "/provider-dispatch-approval-policy-confirmation-envelope", label: "Approval Policy Confirmation", key: "provider-dispatch-approval-policy-confirmation-envelope" },';
  const lines = nav.split("\n");
  let idx = lines.findIndex((line) => line.includes("{ href:") && line.includes("provider-dispatch-approval-candidate-envelope-dashboard"));
  if (idx < 0) idx = lines.findIndex((line) => line.includes("{ href:") && line.includes("provider-dispatch-approval-candidate-envelope-policy"));
  if (idx < 0) idx = lines.findIndex((line) => line.includes("{ href:") && line.includes("provider-dispatch-approval-candidate-envelope"));
  if (idx < 0) idx = lines.findIndex((line) => line.includes("{ href:"));
  if (idx >= 0) lines.splice(idx + 1, 0, route);
  else lines.push(route);
  write(navFile, lines.join("\n"));
}

const pkgFile = "package.json";
const pkg = JSON.parse(read(pkgFile));
pkg.scripts = pkg.scripts || {};
pkg.scripts["phase42:0:patch"] = "node scripts/phase42-0-patch-provider-dispatch-approval-policy-confirmation-envelope.cjs";
pkg.scripts["phase42:0:verify"] = "node scripts/phase42-0-verify-provider-dispatch-approval-policy-confirmation-envelope.cjs";
pkg.scripts["llm:provider-dispatch-approval-policy-confirmation-envelope:verify"] = "node scripts/phase42-0-verify-provider-dispatch-approval-policy-confirmation-envelope.cjs";
write(pkgFile, JSON.stringify(pkg, null, 2) + "\n");
console.log("Phase 42.0 Patch abgeschlossen.");
