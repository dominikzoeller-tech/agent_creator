const fs=require('fs');const path=require('path');const root=process.cwd();const rel='frontend/app/cmt/master/secure/page.tsx';const full=path.join(root,rel);let ok=true;if(!fs.existsSync(full)){console.error('[missing]',rel);process.exit(1)}const text=fs.readFileSync(full,'utf8');if(text.includes('toneColor[badge.tone]')){console.error('[bad] raw toneColor badge index remains');ok=false}else console.log('[ok] raw toneColor badge index removed');if(!text.includes('keyof typeof toneColor')){console.error('[missing] toneColor keyof cast');ok=false}else console.log('[ok] toneColor keyof cast found');if(ok)console.log('[OK] tone color index verify passed');process.exit(ok?0:1);