const fs=require('fs');const path=require('path');const root=process.cwd();let ok=true;const lib=fs.readFileSync(path.join(root,'frontend/lib/cmt-secure-master-agent-mvp.ts'),'utf8');const page=fs.readFileSync(path.join(root,'frontend/app/cmt/master/secure/agent/page.tsx'),'utf8');for(const token of ['priority','liveReadiness','Live-Readiness','blocked_by_privacy']){if(!lib.includes(token)){console.error('[missing lib token]',token);ok=false}else console.log('[ok lib]',token)}for(const token of ['Priorität','Live-Readiness','Live-Schaltung bleibt gesperrt','Nächste Haupt-Entscheidung']){if(!page.includes(token)){console.error('[missing page token]',token);ok=false}else console.log('[ok page]',token)}if(ok)console.log('[OK] mvp-agent-4 verify passed');process.exit(ok?0:1);