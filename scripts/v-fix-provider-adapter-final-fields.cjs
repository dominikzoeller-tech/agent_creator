const fs=require('fs');const path=require('path');const root=process.cwd();const rel='frontend/lib/cmt-secure-master-provider-adapter-contract.ts';const full=path.join(root,rel);let ok=true;if(!fs.existsSync(full)){console.error('[missing]',rel);process.exit(1)}const text=fs.readFileSync(full,'utf8');for(const token of ['activationRequirements','nextStep','nextSafeStep','requestEnvelopePreview','responseEnvelopePreview','createSecureMasterProviderAdapterContract']){if(!text.includes(token)){console.error('[missing token]',token);ok=false}else console.log('[ok token]',token)}if(ok)console.log('[OK] provider adapter final fields verify passed');process.exit(ok?0:1);