const fs=require('fs');const path=require('path');const root=process.cwd();function walk(dir,out=[]){if(!fs.existsSync(dir))return out;for(const name of fs.readdirSync(dir)){const full=path.join(dir,name);const stat=fs.statSync(full);if(stat.isDirectory())walk(full,out);else if(name==='route.ts')out.push(full)}return out}let ok=true;const files=walk(path.join(root,'frontend/app/api/cmt/master/secure'));for(const full of files){const rel=path.relative(root,full);const text=fs.readFileSync(full,'utf8');const bad=text.match(/from ['"](?:../){4,6}lib//);if(bad){console.error('[bad import]',rel,bad[0]);ok=false}else console.log('[ok route]',rel)}if(ok)console.log('[OK] hotfix api import depth verify passed');process.exit(ok?0:1);