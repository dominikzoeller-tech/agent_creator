import { createCapabilityRequest, decideCapabilityRequest, listCapabilityRequests } from "../../../lib/tool-capability-request-store";
import { appendGovernanceAuditEvent } from "../../../lib/governance-audit-store";
export const dynamic = "force-dynamic";
export async function GET(){ try { return Response.json(listCapabilityRequests()); } catch(error){ const message = error instanceof Error ? error.message : "Capability Requests konnten nicht gelesen werden."; return Response.json({ ok:false, error: message }, { status: 500 }); } }
export async function POST(request: Request){ try { const body = await request.json(); const requestedCapability = typeof body.requestedCapability === "string" ? body.requestedCapability : ""; const created = createCapabilityRequest({ requestedCapability, reason: typeof body.reason === "string" ? body.reason : undefined, userInputPreview: typeof body.userInputPreview === "string" ? body.userInputPreview : undefined, riskLevel: body.riskLevel === "low" || body.riskLevel === "medium" || body.riskLevel === "high" ? body.riskLevel : undefined, metadata: body.metadata && typeof body.metadata === "object" ? body.metadata : undefined }); appendGovernanceAuditEvent({ type: "capability_request_created", actor: "api", entityType: "capability-request", entityId: created.id, status: created.status, riskLevel: created.riskLevel, summary: "Capability Request erstellt: " + created.requestedCapability, metadata: { requestedCapability: created.requestedCapability } });
    return Response.json({ ok:true, request: created }); } catch(error){ const message = error instanceof Error ? error.message : "Capability Request konnte nicht erstellt werden."; return Response.json({ ok:false, error: message }, { status: 400 }); } }
export async function PATCH(request: Request){ try { const body = await request.json(); const id = typeof body.id === "string" ? body.id : ""; const status = body.status === "approved" || body.status === "denied" || body.status === "implemented" ? body.status : null; if(!id || !status) return Response.json({ ok:false, error: "id und status approved/denied/implemented sind erforderlich." }, { status: 400 }); const updated = decideCapabilityRequest({ id, status, decisionNote: typeof body.decisionNote === "string" ? body.decisionNote : undefined }); if(!updated) return Response.json({ ok:false, error: "Capability Request nicht gefunden." }, { status: 404 }); appendGovernanceAuditEvent({ type: "capability_request_decided", actor: "api", entityType: "capability-request", entityId: updated.id, status: updated.status, riskLevel: updated.riskLevel, summary: "Capability Request entschieden: " + updated.requestedCapability, metadata: { requestedCapability: updated.requestedCapability } });
    return Response.json({ ok:true, request: updated }); } catch(error){ const message = error instanceof Error ? error.message : "Capability Request konnte nicht entschieden werden."; return Response.json({ ok:false, error: message }, { status: 500 }); } }
