import { createApprovalTokenActivationGate, listApprovalTokenActivationGates, summarizeApprovalTokenActivationGates } from "../../../lib/approval-token-activation-gate-store";
export const dynamic = "force-dynamic";
export async function GET(request: Request){try{const url=new URL(request.url);const limit=Number(url.searchParams.get("limit")||"100");const approvalTokenActivationGates=listApprovalTokenActivationGates(Number.isFinite(limit)?limit:100);return Response.json({ok:true,summary:summarizeApprovalTokenActivationGates(approvalTokenActivationGates),approvalTokenActivationGates});}catch(error){const message=error instanceof Error?error.message:"Approval Token Activation Gates konnten nicht gelesen werden.";return Response.json({ok:false,error:message},{status:500});}}
export async function POST(request: Request){try{const body=await request.json();const gate=createApprovalTokenActivationGate({issuanceGateId:typeof body.issuanceGateId==="string"?body.issuanceGateId:undefined,activationIntentRecorded:body.activationIntentRecorded===true,metadata:body.metadata&&typeof body.metadata==="object"?body.metadata:undefined});return Response.json({ok:true,approvalTokenActivationGate:gate});}catch(error){const message=error instanceof Error?error.message:"Approval Token Activation Gate konnte nicht erstellt werden.";return Response.json({ok:false,error:message},{status:400});}}
